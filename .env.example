# Copy this to .env.local for local overrides
# No secrets needed — all data comes from free public RSS feeds

# Update this to your real domain before going live
SITE_URL=https://transferhub.vercel.app

# Optional: Google Analytics 4 Measurement ID (add after GA4 setup)
# PUBLIC_GA_ID=G-XXXXXXXXXX

# Optional: Google AdSense Publisher ID (add after approval)
# PUBLIC_ADSENSE_ID=ca-pub-XXXXXXXXXXXXXXXX
